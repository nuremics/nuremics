[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nuremics" 
version = "0.0.0b0"
description = "A Python™ programming framework to turn scientific prototypes into production-grade software components."
authors = [
  { name = "Julien SIGUENZA", email = "contact@julien-siguenza.com" }
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "attrs==24.1.0",
  "numpy==2.0.1",
  "pandas==2.2.2",
  "openpyxl==3.1.5",
  "termcolor==3.0.1",
]

[project.urls]
"Homepage" = "https://github.com/nuremics/nuremics.git"